package kh.java.func;

//기본적으로 추상이므로 abstract는 묵시적임 
//따라서 구현하게 되면(상속받았을 경우) 다 오버라이딩 해줘야함
//하위 클래스에서 implements키워드 사용해서 구현받음
public interface test {
	//메소드도 abstarct 안 적어도 됨
	//변수는 public static fianl a
	//객체 생성은 안되지만 참조형 변수로는 가능
}
